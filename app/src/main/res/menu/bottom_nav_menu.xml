<?xml version="1.0" encoding="utf-8"?>
<!-- Menú para la barra de navegación inferior. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Botón de navegación para Home. Su ID coincide con el del fragment en nav_graph. -->
    <item
        android:id="@+id/homeFragment"
        android:icon="@drawable/ic_home"
        android:title="Home" />

    <!-- Botón de navegación alternativo para HomeAdmin (solo visible para empleados). -->
    <item
        android:id="@+id/homeAdminFragment"
        android:icon="@drawable/ic_home"
        android:title="Home" />

    <!-- Botón de navegación para Perfil. Su ID coincide con el del fragment en nav_graph. -->
    <item
        android:id="@+id/profileFragment"
        android:icon="@drawable/ic_profile"
        android:title="Perfil" />

    <!-- Botón de navegación para Carrito -->
    <item
        android:id="@+id/cartFragment"
        android:icon="@drawable/ic_cart"
        android:title="Carrito" />

    <!-- Gestión de Usuarios (solo owner rol_id=2) -->
    <item
        android:id="@+id/manageUsersFragment"
        android:icon="@drawable/ic_profile"
        android:title="Usuarios" />

    <!-- Gestión de Empleados (solo owner rol_id=2) -->
    <item
        android:id="@+id/manageEmployeesFragment"
        android:icon="@drawable/ic_profile"
        android:title="Empleados" />

    <!-- Órdenes (visible para empleados) -->
    <item
        android:id="@+id/manageOrdersFragment"
        android:icon="@drawable/ic_home"
        android:title="Órdenes" />

</menu>
